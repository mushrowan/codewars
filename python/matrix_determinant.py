#!/usr/bin/python3
def determinant(matrix):
    for y, row = list in enumerate(matrix):
        for x, value in enumerate(matrix[y]):
            if len(matrix) == 2:
                return sum([row[x] * matrix[y] for x, y in matri])


    pass
